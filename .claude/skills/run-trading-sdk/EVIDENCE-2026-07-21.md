# Exact-head validation evidence — 2026-07-21

This record makes the local validation claimed by trading-sdk PR #45
reproducible. GitHub's substantive TypeScript, Rust, Python, and Prettier jobs
were path-filter skipped because this PR changes only `.claude/**`; the green
aggregate gate therefore does **not** represent those suites running in GitHub
Actions.

## Source and environment

- PR: <https://github.com/Proof-labs/trading-sdk/pull/45>
- Validated commit: `cb839d7f70b55e1b86de1ed829e1f6fd7da3ef02`
- Base commit: `14bd0224fc9df2a5bbec45eb8f0486f1b3da1bfc`
- Capture completed: `2026-07-21T19:47:21Z`
- Host: `Darwin 25.5.0 arm64`
- Node: `v25.8.1`
- npm: `11.11.0`
- rustc: `1.95.0 (59807616e 2026-04-14)`
- cargo: `1.95.0 (f2d3ce0bd 2026-03-21)`
- Rust target: `wasm32-unknown-unknown`
- wasm-bindgen CLI: `0.2.126`
- Python: `3.11.15`
- maturin: `1.14.1`
- pytest: `9.1.1`

The checkout was clean at the validated commit before the evidence file was
added. All commands below ran from the repository root. Every recorded command
completed with exit code 0.

## Rust, formatting, and authoritative vectors

```sh
cargo fmt --all -- --check
cargo generate-lockfile
cargo test --workspace --exclude proof-trading-sdk-pyo3 --locked
cargo run -p proof-trading-sdk-conformance --bin gen-vectors --locked
git diff --exit-code conformance/
```

Observed results:

- `cargo fmt`: clean.
- Workspace tests: 68 passed, 0 failed — 60 core, 3 conformance-library,
  3 conformance-runner, and 2 derive tests. The WASM crate and doc-test targets
  contained no additional tests.
- Vector generation: wrote 15 codec, 3 signing, and 4 nonce cases.
- `git diff --exit-code conformance/`: clean; regenerated vectors are identical
  to the committed vectors.
- Exit codes in command order: `0, 0, 0, 0, 0`.

## Python binding and conformance

The test environment was a newly created Python 3.11 virtual environment. The
equivalent replay commands are:

```sh
python3.11 -m venv .venv-evidence
PATH="$PWD/.venv-evidence/bin:$PATH"
export PATH
python -m pip install --upgrade pip
pip install 'maturin>=1.7,<2' pytest
pip install ./python --no-build-isolation
pytest python/tests -q
```

Observed results:

- The PyO3 wheel built and installed as `proof-trading-sdk==2.0.0`.
- `pytest`: 64 passed, 0 failed in 1.08 seconds.
- Final build/install and test exit codes: `0, 0`.

An initial setup attempt invoked the virtual environment's `pip` by absolute
path without placing that environment's `bin` directory on `PATH`; maturin's
PEP 517 subprocess therefore could not resolve `maturin` and exited 1 before
building the package. The clean replay above uses normal activated-environment
PATH semantics and is the authoritative successful run.

## TypeScript, WASM, offline driver, and package

```sh
npm ci --ignore-scripts
npm run build
node .claude/skills/run-trading-sdk/driver.mjs
npm test
npm pack --dry-run
```

Observed results:

- `npm ci`: 53 packages installed, 0 vulnerabilities.
- WASM release build, wasm-bindgen generation, TypeScript compilation, and WASM
  copy completed successfully.
- Offline driver: all 19 checks passed, including WASM readiness, key/owner
  derivation, payload encoding, signed-envelope encoding, decoding, and
  deterministic re-encoding.
- Vitest: 197 passed, 18 intentionally skipped; 9 test files passed and 12
  RPC-gated files skipped.
- Package dry run: 43 files, including one
  `dist/wasm/proof_trading_sdk_wasm_bg.wasm` artifact.
- Exit codes in command order: `0, 0, 0, 0, 0`.

## Packed Vite consumer and Chromium

```sh
npm run smoke:browser:prepare
npm run smoke:browser
npx prettier --check \
  .claude/skills/run-trading-sdk/SKILL.md \
  .claude/skills/run-trading-sdk/driver.mjs
git diff --check
```

Observed results:

- Packed-consumer build emitted exactly one WASM asset:
  `dist/assets/proof_trading_sdk_wasm_bg-DS5xARZ_.wasm`.
- Chromium fetched the packaged WASM with HTTP 200 and produced the canonical
  `PlaceOrder` bytes
  `9901dc00140101010101010101010101010101010101010101a3427579640ac0c2c2a3477463`.
- Prettier and whitespace checks were clean.
- Exit codes in command order: `0, 0, 0, 0`.

## GitHub Actions context

At the validated head, GitHub Actions run
<https://github.com/Proof-labs/trading-sdk/actions/runs/29859328858> reported the
detector, CI gate contract, and aggregate `ci` checks green, while the
TypeScript, Rust, Python, and Prettier jobs were skipped by the repository's
existing path filter. This evidence file does not modify that CI policy; it
records the full local contract for reviewer replay.
