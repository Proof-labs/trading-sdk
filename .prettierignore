# Build output / vendored
dist/
node_modules/
target/
fuzz/target/

# Non-JS/TS configs prettier has no parser for
*.toml
Cargo.lock
package-lock.json

# Python + Rust sources (formatted by their own toolchains)
python/
crates/
fuzz/
conformance/

# Agent-config files synced from Proof-labs/.github (org canon owns their
# formatting; local prettier churn would re-break every sync)
CLAUDE.md
AGENTS.md
.github/copilot-instructions.md
